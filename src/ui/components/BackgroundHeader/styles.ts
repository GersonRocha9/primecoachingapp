import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    alignItems: 'center',
    justifyContent: 'center',
  },
  copyrightContainer: {
    position: 'absolute',
    right: 32,
  },
  iconContainer: {
    position: 'absolute',
    bottom: -16,
    left: 32,
    padding: 16,
    borderRadius: 999,
  },
})

